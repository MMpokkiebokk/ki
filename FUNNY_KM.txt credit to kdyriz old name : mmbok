function x(tt, tx, drtion)
        game.StarterGui:SetCore(
            "SendNotification",
            {
                Title = tt,
                Text = tx,
                Duration = drtion
            }
        )
    end
x("wait for fixing","away update and work"100000)
